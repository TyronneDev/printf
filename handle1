#include <stdio.h>
#include "main.h"
#include <unistd.h>

/**
*_printf(const char *format, ...):function that produces output according to a
* format.
*Returns: the number of characters printed (excluding the null byte used to end
* output to strings)
/format is a character string. 
*/
int _printf(const char *format, ...)
{
int i = 0;


return (i);
}
